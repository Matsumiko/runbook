interface:
  display_name: "Calendar Builder"
  short_description: "Build calendar and schedule views"
  default_prompt: "Use frontend-calendar-builder to build calendar and schedule UI that follows the existing stack, theme, and product UI DNA."
