# Properties, Metadata, And Actions

- Group fields by meaning rather than by raw data order.
- Keep high-value metadata above long technical lists.
- Use inline edits only when the surrounding product already supports that pattern.
- Keep contextual actions near the metadata they affect.
- Avoid turning the inspector into a full detail page or settings screen.
