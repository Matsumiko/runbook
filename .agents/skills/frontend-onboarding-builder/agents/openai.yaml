interface:
  display_name: "Onboarding Builder"
  short_description: "Build first-run onboarding flows on existing UI foundations"
  default_prompt: "Use $frontend-onboarding-builder to build onboarding flows that follow the existing stack, tokens, and product UI DNA."
