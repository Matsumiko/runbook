# Breadcrumb Trail Contract

Define the trail before writing UI.

Clarify:

- which hierarchy the breadcrumb represents
- whether each ancestor is navigable
- how the current page should differ from its ancestors
- whether labels come from routes, records, or filtered context
- whether a collapsed ancestor pattern is required

Use breadcrumbs only when ancestry helps orientation or backtracking.
