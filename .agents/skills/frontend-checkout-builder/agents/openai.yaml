interface:
  display_name: "Checkout Builder"
  short_description: "Build checkout flows on existing UI foundations"
  default_prompt: "Use $frontend-checkout-builder to build checkout flows that follow the existing stack, tokens, and product UI DNA."
