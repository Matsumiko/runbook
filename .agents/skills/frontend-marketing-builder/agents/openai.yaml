interface:
  display_name: "Marketing Builder"
  short_description: "Build marketing pages on existing UI foundations"
  default_prompt: "Use $frontend-marketing-builder to build marketing pages that follow the existing stack, tokens, and product UI DNA."
